# =============================================================================
# COPIAR / MESCLAR NO .env DO VPS (produção) — bloco Platlog / eSupri
# Arquivo: RAIZ do projeto no servidor (o mesmo .env do docker-compose)
# NÃO use backend/.env no Docker — o compose só lê ./.env
# =============================================================================
# Depois: docker compose up -d --force-recreate app
#         e na tela Configurações → Sync NF: Rodar agora (ou espera 05:00)
# =============================================================================

ESUPRI_USER=VERONICA
ESUPRI_PASS=
ESUPRI_BASE_URL=https://www.esupri.com.br
ESUPRI_USE_CHROME=0
ESUPRI_SYNC_NFE=1
ESUPRI_SYNC_CATALOGO=0
