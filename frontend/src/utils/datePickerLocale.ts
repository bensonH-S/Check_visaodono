import { ptBR } from '@mui/x-date-pickers/locales';

/** Textos do DatePicker / DateCalendar em português (evita "Select date"). */
export const datePickerPtBR =
  ptBR.components.MuiLocalizationProvider.defaultProps.localeText;
